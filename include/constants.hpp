#ifndef CONSTANTS
#define CONSTANTS

#include <string>
#include <SFML/System.hpp>

// FILES
const std::string DATA_PATH = "data/";

// WINDOW
const sf::Vector2f DEFAULT_SCREEN_SIZE(800, 600);
const unsigned int FRAMERATE_LIMIT = 30;
const unsigned int MAX_VALUE = 200;
const unsigned int BORDER_SIZE = 25;

//COLORS

const sf::Color BROWN(120, 88, 7);

//
const float CAR_DEFAULT_SPEED = 20;
#endif //CONSTANTS